{% extends 'base.html.twig' %}

{% block title %}{{ 'edit'|trans}} {{ 'label_wallet'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'edit'|trans}} {{ 'label_wallet'|trans }}</h1>

    {{ include('wallet/_form.html.twig', {'button_label': 'Zapisz'}) }}<br>

    {{ include('wallet/_delete_form.html.twig', {'button_label': 'Usuń'}) }}

    <br><a href="{{ path('wallet_index') }}" class="btn btn-outline-success">{{ 'back'|trans }}</a>

{% endblock %}
