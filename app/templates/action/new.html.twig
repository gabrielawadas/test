{% extends 'base.html.twig' %}

{% block title %}{{ 'label_new_action'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'label_new_action'|trans }}</h1>

    {{ include('action/_form.html.twig') }}<br>

    <a href="{{ path('wallet_index') }}" class="btn btn-outline-primary">{{ 'back'|trans }}</a>
{% endblock %}
